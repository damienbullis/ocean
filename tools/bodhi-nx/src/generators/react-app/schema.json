{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "library",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "React app name",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the React application?",
      "pattern": "^[a-zA-Z].*$"
    }
  },
  "required": ["name"]
}
